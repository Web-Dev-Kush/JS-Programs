//Find Area of triangle

function AreaOfTriangle(base, height) {
  let area = 0.5 * base * height;
  console.log(
    `The area of triangle of base ${base} and height ${height} is`,
    area, "sq m"
  );
}

AreaOfTriangle(5,6);
